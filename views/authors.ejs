<!DOCTYPE html>
<html lang="en">

<%- include('blocks/head.html') %>

<body>

    <%- include('blocks/header.html') %>

        <main>

            <div class="container">

                <div class="section-header">
                    <h1 class="subtitle">Authors</h1>

                    <span></span>
                    
                    <div class="button">
                        <a class="button-link" href="/authors/new">Add author</a>
                    </div>
                </div>

                <% if (authors.length === 0)  { %>
                    <p>There are no authors registered yeet!</p>
                <% } else { %>

                <div class="field-container">
                    <input placeholder="Type here to search for an author..." class="filter-input input" type="text">
                </div>

                <table class="table">
                    <thead class="table-header">
                        <tr>
                            <% for (let key in authors[0]) {%>
                                <th class="table-header-field">
                                    <%= key %>
                                </th>
                            <% } %>
                            <th class="table-header-field">
                                link
                            </th>
                        </tr>
                    </thead>
                    <tbody class="table-body">
                        <% for (let author of authors) {%>
                            <tr>
                                <% for (let key in author) {%>
                                    <td class="table-body-field">
                                        <%= author[key] %>
                                    </td>
                                <% } %>
                                <td class="table-body-field">
                                    <a class="link" href="/authors/<%= author.id %>">See author</a>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>

                <% } %>
            
            </div>

        </main>

</body>

<%- include('blocks/scripts.html')%>

</html>